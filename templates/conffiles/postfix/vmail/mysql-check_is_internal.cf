user = {{ dbuser }}
password = {{ dbpass }}
dbname = {{ dbname }}
hosts = {{ dbhost }}

query = SELECT 'OK' FROM user u JOIN domain d ON u.domain_id = d.id WHERE CONCAT(u.user,'@',d.name) = '%s' AND d.name = '%d'
