    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <li><a class="btn btn-secondary" href="{{ path('logout') }}">{% trans %}Logout{% endtrans %} {{ app.user }}</a></li>
    {% else %}
        <li><a href="{{ path('login') }}">{% trans %}Log in{% endtrans %}</a></li>
    {% endif %}
