    {# Menú de usuario #}
    {% if is_granted('ROLE_USER') %}
        <li class="nav-item dropdown">
          <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user }}<span class="caret"></span></a>
          <div class="dropdown-menu">
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="{{ path('user_self_edit')}}">{% trans %}User profile{% endtrans %}</a>
          </div>
        </li>
    {% endif %}
