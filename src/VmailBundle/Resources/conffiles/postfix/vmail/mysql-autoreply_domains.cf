user = {{ dbuser }}
password = {{ dbpass }}
dbname = {{ dbname }}
hosts = {{ dbhost }}
#query = SELECT CONCAT('autoreply.',domain) AS virtdom FROM domains WHERE domain=SUBSTRING_INDEX('%s','.',-2)

query = SELECT CONCAT('autoreply.',name) AS autoreplydom FROM domain WHERE name='%s' AND id !=0 AND is_active=1
